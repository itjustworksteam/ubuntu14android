language: python

python:
  - 2.7

build:
  pre_ci:
    - docker pull therickys93/ubuntu14java
    - docker build --rm -t therickys93/ubuntu14android .

  pre_ci_boot:
    image_name: therickys93/ubuntu14android
    image_tag: latest
    pull: false

  ci:
    - bash /android/test/test.sh